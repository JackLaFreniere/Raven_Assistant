"""
AI-based intent detection using language models.
Implementation to be added later.
"""

from typing import Dict, Optional


def process_ai(text: str) -> Dict[str, Optional[str]]:
    """
    Detects intent and extracts payload using AI/language model.
    
    Args:
        text: The user command text
    
    Returns:
        Dict with "intent" and "payload" keys
    """
    
    # TODO: Implement AI-based intent detection
    # This will use an LLM or similar to analyze the command
    
    return {"intent": "unknown", "payload": text}
